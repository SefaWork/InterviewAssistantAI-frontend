# React Frontend for InterviewAssistantAI Project

## Purpose
This package creates user interface, taking in data from Django api endpoints. This package is concerned with routing and UI and is not concerned with business logic.

## Setup
Make sure you have Node.js and NPM installed.

### Install packages.
```powershell
cd frontend
npm install
```

### Test the project.
```powershell
npm run dev
```